/**
 * Site-wide custom scripts for emergencymode.news
 *
 * Loaded via the child theme (themes/emfn-child/functions.php) or
 * through Newspack > Customization > Additional JS.
 */

( function () {
    'use strict';

    // Example: log load confirmation during development.
    // Remove or replace before going to production.
    // console.log( '[EMFN] Custom JS loaded.' );
} )();
