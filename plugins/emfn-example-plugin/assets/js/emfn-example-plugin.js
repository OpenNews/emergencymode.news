/**
 * EMFN Example Plugin – front-end scripts
 *
 * Add plugin-specific JavaScript below.
 * The file is loaded in the footer (true passed as last arg to wp_enqueue_script).
 */

( function () {
    'use strict';
} )();
